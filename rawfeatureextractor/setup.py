from setuptools import setup

setup(
    name='raw-feature-extractor',
    version='1.0.0',
    description='Extract Raw Features from binary files or source code files',
    install_requires=['networkx'],
    packages=['extractor']
)